---
layout: single
title:  "Docker를 활용한 가상머신서버만들기"
---

# Docker

## 구조
Hub -- Image -- Container  
  pull    run

docker images --list  
docker pull  
docker password  
docker start  
docker stop  
docker rmi 이미지명  
docker rm (--force) 컨테이너명  
docker logs -f 이름  
docker run 80:80 이미지명
